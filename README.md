# pH Sensor Extension v1.0.0

**Analog pH Sensor for MicroBlock/MicroPython**

Linear Calibration with Two-Point Method (pH 4.0 & pH 7.0)

---

## 📖 ภาพรวม

Extension สำหรับวัดค่า pH ด้วย Analog pH Sensor โดยใช้การ Calibrate แบบ 2 จุด (Two-Point Calibration)

### เหมาะสำหรับ:
- ✅ ระบบไฮโดรโปนิกส์ (Hydroponics)
- ✅ ตู้ปลา (Aquarium)
- ✅ ตรวจสอบคุณภาพน้ำ
- ✅ งานทดลองวิทยาศาสตร์
- ✅ เกษตรอัจฉริยะ (Smart Farming)

---

## 🌟 คุณสมบัติ

1. **Linear Calibration**
   - สมการเส้นตรง: pH = m*V + c
   - Two-Point Calibration (pH 4.0 และ pH 7.0)
   - Auto-save calibration

2. **Median Filtering**
   - อ่านค่า 10 samples
   - กรองค่าผิดปกติ
   - ค่าเสถียร

3. **8 Blockly Blocks**
   - ใช้งานง่าย
   - รองรับทุกฟังก์ชัน
   - Calibrate ได้ใน Block

4. **Dual API**
   - Simple API
   - Class API

---

## 🔌 การเชื่อมต่อ Hardware

### การต่อสาย:

```
pH Sensor       →    ESP32
───────────────────────────
VCC (Red)       →    5V
GND (Black)     →    GND
Signal (Blue)   →    GPIO 35
```

### ⚠️ ข้อควรระวัง:
- ใช้ **GPIO 32-39** (ADC1)
- pH Sensor ต้องการ **5V** (ไม่ใช่ 3.3V)
- อย่าปล่อยให้เซ็นเซอร์แห้ง

---

## 📦 โครงสร้างไฟล์

```
ph-sensor/
├── ph_extension.js      # Extension config
├── ph_blocks.js         # Blockly blocks
├── ph_generators.js     # Python generators
├── modules/
│   └── ph_sensor.py     # Python module
└── README.md           # เอกสารนี้
```

---

## 🚀 การใช้งาน

### Simplified API:

```python
import ph_sensor

# อ่านค่า pH
ph = ph_sensor.get_ph_value(35)
print(f"pH: {ph}")

# อ่านแรงดัน
voltage = ph_sensor.get_voltage(35)
print(f"Voltage: {voltage} V")
```

### Class API (Arduino Style):

```python
import ph_sensor
import time

# Setup
sensor = ph_sensor.init(pin=35)

# Loop
while True:
    sensor.update()
    ph = sensor.get_ph_value()
    print(f"pH: {ph:.2f}")
    time.sleep(1)
```

---

## 🧩 Blockly Blocks

### 8 Blocks ทั้งหมด:

1. **อ่านค่า pH ขา [pin]** - สีแดง
2. **อ่านแรงดัน pH Sensor ขา [pin]** - สีม่วง
3. **ตั้งค่า pH Calibration Slope/Intercept** - สีส้ม
4. **Calibrate pH ขา [pin] แรงดัน pH 4.0/7.0** - สีเขียว
5. **ดูค่า pH Calibration** - สีเทา
6. **อ่านค่า pH ทั้งหมด (Dict) ขา [pin]** - สีฟ้า
7. **แสดงค่า pH Sensor ขา [pin]** - สีน้ำเงินเข้ม
8. **ตรวจสอบระดับ pH [value]** - สีเขียวน้ำทะเล

---

## 🔬 การ Calibrate

### Two-Point Calibration

การ Calibrate pH Sensor ต้องใช้สารละลาย 2 แบบ:
- **pH 4.0 Buffer Solution** (สีแดง/ชมพู)
- **pH 7.0 Buffer Solution** (สีเขียว/เหลือง)

### ขั้นตอน Calibrate:

```python
import ph_sensor

sensor = ph_sensor.init(pin=35)

# Step 1: จุ่มเซ็นเซอร์ใน pH 4.0 buffer
# รอ 30 วินาที
sensor.update()
ph4_voltage = sensor.get_voltage()
print(f"pH 4.0 voltage: {ph4_voltage}")

# Step 2: ล้างเซ็นเซอร์ด้วยน้ำกลั่น
# จุ่มเซ็นเซอร์ใน pH 7.0 buffer
# รอ 30 วินาที
sensor.update()
ph7_voltage = sensor.get_voltage()
print(f"pH 7.0 voltage: {ph7_voltage}")

# Step 3: Calibrate
sensor.calibrate_two_point(ph4_voltage, ph7_voltage)

# Output:
# ==================================================
# Two-Point Calibration Complete!
# ==================================================
# pH 4.0 voltage:  2.50 V
# pH 7.0 voltage:  2.00 V
# New Slope (m):   -6.0000
# New Intercept (c): 19.0000
# ==================================================
```

### หรือใช้ Blockly:

```blockly
// Step 1: อ่านแรงดัน pH 4.0
ph4_v = อ่านแรงดัน pH Sensor ขา [35]

// Step 2: อ่านแรงดัน pH 7.0
ph7_v = อ่านแรงดัน pH Sensor ขา [35]

// Step 3: Calibrate
Calibrate pH ขา [35] แรงดัน pH 4.0 [ph4_v] แรงดัน pH 7.0 [ph7_v]
```

---

## 📊 ค่ามาตรฐาน pH

### ระดับ pH:

| ระดับ pH | คำอธิบาย | ตัวอย่าง |
|----------|----------|----------|
| 0-3 | กรดจัด | กรดแบตเตอรี่, น้ำส้ม |
| 3-6 | กรดอ่อน | กาแฟ, มะนาว |
| 7 | กลาง (Neutral) | น้ำบริสุทธิ์ |
| 8-10 | ด่างอ่อน | น้ำทะเล, สบู่ |
| 10-14 | ด่างจัด | น้ำยาซักผ้า |

### pH สำหรับไฮโดรโปนิกส์:

| พืช | pH ที่เหมาะสม |
|-----|---------------|
| ผักสลัด | 5.5-6.5 |
| มะเขือเทศ | 5.5-6.5 |
| สตรอว์เบอร์รี่ | 5.5-6.5 |
| แตงกวา | 5.5-7.0 |
| พริก | 6.0-6.5 |

### pH สำหรับตู้ปลา:

| ประเภท | pH ที่เหมาะสม |
|--------|---------------|
| ปลาน้ำจืด | 6.5-7.5 |
| ปลาทะเล | 8.0-8.4 |
| กุ้ง | 7.0-8.0 |
| ปะการัง | 8.1-8.4 |

---

## 💡 ตัวอย่างโปรเจค

### 1. ระบบควบคุม pH ไฮโดรโปนิกส์

```python
import ph_sensor
import machine
import time

# Setup
sensor = ph_sensor.init(pin=35)
pump_ph_down = machine.Pin(26, machine.Pin.OUT)  # ปั๊มลด pH
pump_ph_up = machine.Pin(27, machine.Pin.OUT)    # ปั๊มเพิ่ม pH

# Target pH range (สำหรับผักสลัด)
TARGET_PH_MIN = 5.5
TARGET_PH_MAX = 6.5

while True:
    sensor.update()
    ph = sensor.get_ph_value()
    
    print(f"pH: {ph:.2f}", end=" ")
    
    if ph < TARGET_PH_MIN:
        print("⚠️ pH ต่ำเกินไป - เพิ่ม pH UP")
        pump_ph_up.on()
        time.sleep(2)  # ปั๊ม 2 วินาที
        pump_ph_up.off()
        time.sleep(300)  # รอ 5 นาทีให้ผสมกัน
        
    elif ph > TARGET_PH_MAX:
        print("⚠️ pH สูงเกินไป - เพิ่ม pH DOWN")
        pump_ph_down.on()
        time.sleep(2)
        pump_ph_down.off()
        time.sleep(300)
        
    else:
        print("✅ pH เหมาะสม")
    
    time.sleep(600)  # ตรวจสอบทุก 10 นาที
```

### 2. ตรวจสอบคุณภาพน้ำดื่ม

```python
import ph_sensor

sensor = ph_sensor.init(pin=35)

while True:
    sensor.update()
    ph = sensor.get_ph_value()
    
    # ประเมินคุณภาพ (น้ำดื่มที่ดีควรอยู่ที่ pH 6.5-8.5)
    if ph < 6.5:
        quality = "กรดเกินไป ❌"
    elif ph <= 8.5:
        quality = "คุณภาพดี ✅"
    else:
        quality = "ด่างเกินไป ❌"
    
    print(f"pH: {ph:.2f} | {quality}")
    time.sleep(5)
```

---

## 🛠️ Troubleshooting

### ❓ ปัญหาที่พบบ่อย

#### 1. ค่า pH ไม่เปลี่ยน
**สาเหตุ**: เซ็นเซอร์แห้ง  
**วิธีแก้**: แช่เซ็นเซอร์ในน้ำกลั่น 24 ชั่วโมง

#### 2. ค่าไม่แม่นยำ
**สาเหตุ**: ยังไม่ Calibrate  
**วิธีแก้**: Calibrate ด้วย pH 4.0 และ pH 7.0

#### 3. ค่ากระโดก
**สาเหตุ**: สัญญาณรบกวน  
**วิธีแก้**: ใช้สายที่สั้นลง ห่างจากมอเตอร์

#### 4. อ่านค่าไม่ได้
**สาเหตุ**: ต่อสายผิด  
**วิธีแก้**: ตรวจสอบว่า Signal ต่อที่ GPIO 35

---

## 📝 Best Practices

### ✅ ควรทำ:
1. Calibrate ก่อนใช้งานครั้งแรก
2. Calibrate ใหม่ทุก 1-2 เดือน
3. เก็บเซ็นเซอร์ในน้ำกลั่นเสมอ
4. รอให้ค่าคงที่ก่อนอ่าน (30 วินาที)
5. ล้างเซ็นเซอร์ระหว่าง Calibrate

### ❌ ไม่ควรทำ:
1. อย่าปล่อยให้เซ็นเซอร์แห้ง
2. อย่าใช้ในน้ำร้อน >60°C
3. อย่าขูดหัวเซ็นเซอร์
4. อย่าใช้สารเคมีแรงล้าง
5. อย่าเก็บนอกน้ำนาน >1 ชั่วโมง

---

## 📚 API Reference

### Class API
```python
sensor = PHSensor()
sensor.set_pin(35)
sensor.begin()
sensor.update()
ph = sensor.get_ph_value()
voltage = sensor.get_voltage()
sensor.calibrate_two_point(ph4_v, ph7_v)
sensor.set_calibration(slope, intercept)
sensor.print_info()
```

### Simplified API
```python
ph = get_ph_value(pin)
voltage = get_voltage(pin)
calibrate_two_point(pin, ph4_v, ph7_v)
set_calibration(slope, intercept)
print_readings(pin)
```

---

## 🔗 ข้อมูลเพิ่มเติม

- pH Buffer Solutions: [Amazon](https://www.amazon.com/s?k=pH+calibration+buffer)
- ESP32 ADC: [Documentation](https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-reference/peripherals/adc.html)

---

## 📄 License

MIT License

---

## 👤 Author

**Cap_Apiluk**
- Version: 1.0.0
- Category: Sensor

---

**Happy Measuring! 🧪**
